python3 -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env





Command ||  Input || What it verifies

python task2_mcp/demo.py ||  None || Five backend tools + append to audit_log.jsonl






python -m task2_mcp.client get_order_status '{"order_id": "ord_10042"}'  ||  Tool name + JSON args || MCP stdio server + client





python task5_nova_platform.py ||  Env NOVA_SAMPLE_TICKET (optional); default ticket in file ||  LangGraph + tools/RAG + Groq synthesis





python task5_demo.py || None (fixed 3 tickets) || Writes nova_traces.json (uses Groq)





python scripts/export_langgraph_png.py || None (needs graphviz for PNG) || Regenerates nova_agent_graph.png or .mmd fallback





Notebooks (task1, task3, task4) ||   Colab/local kernel + .env or secrets || Task 1/3/4 as designed for Colab